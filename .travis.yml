language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-wpgtx' ]; then cd .. && eval "mv $currentfolder generator-wpgtx" && cd generator-wpgtx; fi

